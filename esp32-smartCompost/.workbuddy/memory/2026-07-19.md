# 2026-07-19

## 项目改动：临时移除 DS18B20 与 SHT31 传感器
- 用户只保留 CO₂(MH-Z16) 与 O₂(DFRobot) 两套传感器，其余传感器物理拆除。
- 采用「注释而非删除」方式保留原代码，便于以后恢复：
  - `src/sensor.h`：注释 `readDS18B20()` / `readSHT30Temp()` / `readSHT30Hum()` 声明。
  - `src/sensor.cpp`：注释对应 includes、全局对象、init 段、读取函数实现。
  - `src/main.cpp` `doMeasurementAndSave()`：注释 `t_ds/t_air/h_air` 读取及 `RoomTemp/AirTemp/AirHumidity` 三个上报通道；把 O₂ 通道结尾逗号 `},` 改为 `}`（否则 JSON 出现尾逗号非法）。
- 抽气泵/曝气泵作为执行器保留（采样 CO₂/O₂ 仍需抽气）。
- 当前 telemetry 仅含 CO2、O2 两个 channel。
- 注意：`platformio.ini` 中 OneWire/DallasTemperature/Adafruit SHT31 依赖现已未被使用，可后续清理（暂未动）。
- 本机无 PlatformIO，未做编译验证；改动为纯注释，确认无活动代码引用被注释符号。
